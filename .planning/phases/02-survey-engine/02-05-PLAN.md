---
phase: 02-survey-engine
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260315000005_phase2_seed.sql
autonomous: true
requirements:
  - SURVEY-13
  - DIM-01

must_haves:
  truths:
    - "A complete diagnostic survey exists in the DB covering all 12 organizational dimensions"
    - "The survey has company-wide sections (target_roles=['all']) and role-specific sections"
    - "Every question is mapped to 1-3 dimensions in question_dimension_map"
    - "The survey is seeded in 'open' status so employees can immediately take it in testing"
    - "Sections cover all 12 dimensions: organizational clarity, sales-to-engineering handover, architecture & technical governance, engineering productivity, team structure & work allocation, delivery & project management, quality & testing, career growth, leadership & management, culture & work environment, innovation & learning, overall satisfaction"
  artifacts:
    - path: "supabase/migrations/20260315000005_phase2_seed.sql"
      provides: "Enhanced seed with complete diagnostic survey, all dimension mappings, role-specific sections"
      contains: "INSERT INTO surveys"
  key_links:
    - from: "supabase/migrations/20260315000005_phase2_seed.sql"
      to: "question_dimension_map table"
      via: "INSERT statements linking question UUIDs to dimension UUIDs"
      pattern: "question_dimension_map"
---

<objective>
Create a Phase 2 seed migration that produces a complete, open diagnostic survey with all 12 organizational dimensions, company-wide and role-specific sections, and full dimension mappings — ready for testing analytics in Phase 3.

Purpose: Phase 1 seed has a draft survey with 60 questions but it needs status='open', complete dimension mappings, and role-targeted sections for Phase 2 testing.
Output: supabase/migrations/20260315000005_phase2_seed.sql that can be applied on top of Phase 1 seed.
</objective>

<execution_context>
@/Users/xavierpraveen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavierpraveen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-survey-engine/02-CONTEXT.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

@supabase/migrations/20260315000004_seed.sql

<interfaces>
<!-- Phase 1 seed UUID scheme (hardcoded, stable across resets): -->
-- Dimensions: 44400000-0000-0000-0000-000000000001 through ...000012
-- Survey: 55500000-0000-0000-0000-000000000001 (status: draft in Phase 1 seed)
-- Sections: 66600000-0000-0000-0000-000000000001 through ...000013
-- Questions: 77700000-0000-0000-0000-000000000001 through ...000060
-- Users: 33300000-0000-0000-0000-000000000001 through ...000018

The 12 dimensions in order (from requirements):
1. Organizational Clarity (44400000-...-000001)
2. Sales-to-Engineering Handover (44400000-...-000002)
3. Architecture & Technical Governance (44400000-...-000003)
4. Engineering Productivity (44400000-...-000004)
5. Team Structure & Work Allocation (44400000-...-000005)
6. Delivery & Project Management (44400000-...-000006)
7. Quality & Testing (44400000-...-000007)
8. Career Growth (44400000-...-000008)
9. Leadership & Management (44400000-...-000009)
10. Culture & Work Environment (44400000-...-000010)
11. Innovation & Learning (44400000-...-000011)
12. Overall Satisfaction (44400000-...-000012)

Note: Phase 1 seed has 13 sections (one per dimension + 1 general). Questions 77700000...000001 through ...000060 already exist. This migration updates the survey status and adds dimension mappings.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 2 seed migration — update survey status and add dimension mappings</name>
  <files>supabase/migrations/20260315000005_phase2_seed.sql</files>
  <action>
    Read `supabase/migrations/20260315000004_seed.sql` first to understand existing questions, sections, and dimension IDs. Then create the Phase 2 seed migration.

    The migration does three things:

    **1. Update survey status to 'open'**
    ```sql
    UPDATE surveys
    SET status = 'open',
        opens_at = NOW() - INTERVAL '1 day',
        closes_at = NOW() + INTERVAL '30 days'
    WHERE id = '55500000-0000-0000-0000-000000000001';
    ```

    **2. Add role-specific sections for engineering/technical roles**
    Insert 2-3 new sections with target_roles = ARRAY['engineering','qa','architects'] covering technical-specific questions (e.g., code review process, deployment confidence, technical debt awareness). Use new UUIDs starting with 66600000-0000-0000-0000-000000000014 etc. Insert 3-5 questions per new section using new UUIDs 77700000-0000-0000-0000-000000000061 onwards.

    **3. Insert question_dimension_map rows linking all 60+ questions to dimensions**
    Map questions to their parent dimension section's dimension. Each question gets 1 primary dimension mapping. High-level questions (overall satisfaction section) get mapped to dimension 12. Engineering productivity questions get mapped to dimension 4. Etc.

    Map 1 primary dimension per question for all 60 existing questions. For 5-10 cross-cutting questions (e.g., career growth questions that also touch leadership), add a second dimension mapping.

    Use the stable UUID scheme: dimension IDs 44400000-0000-0000-0000-000000000001 through ...000012, question IDs 77700000-0000-0000-0000-000000000001 through ...000060.

    Include at a comment header:
    ```sql
    -- Phase 2 seed: survey activation + dimension mappings
    -- Idempotent: ON CONFLICT DO NOTHING on all inserts
    -- Run after: 20260315000004_seed.sql
    ```

    Make all INSERTs idempotent with ON CONFLICT DO NOTHING. Make the UPDATE idempotent by checking current status first (or just use UPDATE — re-running is harmless since it sets same values).

    After all dimension_map inserts, verify the count with a DO block:
    ```sql
    DO $$
    DECLARE
      mapped_count INTEGER;
    BEGIN
      SELECT COUNT(*) INTO mapped_count FROM question_dimension_map;
      IF mapped_count < 60 THEN
        RAISE EXCEPTION 'Expected at least 60 question_dimension_map rows, got %', mapped_count;
      END IF;
    END $$;
    ```
  </action>
  <verify>
    <automated>cd /Users/xavierpraveen/Documents/Projects/surveyer && supabase db reset 2>&1 | tail -5 && psql "${DATABASE_URL:-postgresql://postgres:postgres@127.0.0.1:54322/postgres}" -c "SELECT status, opens_at, closes_at FROM surveys WHERE id='55500000-0000-0000-0000-000000000001';" 2>/dev/null && psql "${DATABASE_URL:-postgresql://postgres:postgres@127.0.0.1:54322/postgres}" -c "SELECT COUNT(*) as dimension_mappings FROM question_dimension_map;" 2>/dev/null || echo "Supabase not running — verify manually after supabase db reset"</automated>
  </verify>
  <done>Migration applies cleanly after supabase db reset. Survey status is 'open'. question_dimension_map has 60+ rows covering all questions. DO block assertion passes without exception.</done>
</task>

</tasks>

<verification>
- `supabase db reset` applies all migrations without errors
- `SELECT status FROM surveys WHERE id='55500000-...-000001'` returns 'open'
- `SELECT COUNT(*) FROM question_dimension_map` returns >= 60
- `SELECT COUNT(DISTINCT dimension_id) FROM question_dimension_map` returns 12 (all dimensions covered)
- `SELECT COUNT(*) FROM survey_sections WHERE target_roles @> ARRAY['all']::text[]` returns >= 11 (most sections company-wide)
- Phase 1 SQL audit scripts still pass: supabase/tests/anonymity_audit.sql, rls_check.sql, schema_check.sql, seed_check.sql
</verification>

<success_criteria>
- Diagnostic survey is in 'open' state and accessible to all employees
- All 60+ questions have dimension mappings
- All 12 organizational dimensions are represented in mappings
- Role-specific sections exist for engineering/technical roles
- Migration is idempotent and safe to re-run
- Existing Phase 1 seed validation scripts still pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-survey-engine/02-05-SUMMARY.md`
</output>
