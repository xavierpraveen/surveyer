---
phase: 06-critical-bug-fixes
plan: 03
type: execute
wave: 2
depends_on:
  - 06-02
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements:
  - AUTH-06

must_haves:
  truths:
    - "AUTH-06 checkbox is [x] in REQUIREMENTS.md with an inline note explaining v1 consolidation"
    - "DASH-01, DASH-02, DASH-03, DASH-04, ANALYTICS-04, ADMIN-02, ADMIN-03 checkboxes are [x]"
    - "Coverage count at the bottom reads 99/99 (or the correct implemented count after AUTH-06 is closed)"
    - "The Traceability table has AUTH-06 mapped to Phase 6 with status Complete"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirement checkbox state reflecting all implemented work"
      contains: "[x] **AUTH-06**"
---

<objective>
Sync REQUIREMENTS.md checkboxes to reflect reality. The audit found 7 requirements implemented but still marked `[ ]`: DASH-01–04, ANALYTICS-04, ADMIN-02, ADMIN-03. AUTH-06 is now implemented by Plan 02 of this phase. Update checkboxes, update the coverage count, and update the traceability table.

Purpose: REQUIREMENTS.md is the source of truth for v1 completion status. Stale checkboxes create false signals about what is still pending.
Output: REQUIREMENTS.md with 99/99 requirements checked (or correct implemented count), AUTH-06 explicitly noted as v1 consolidation complete.
</objective>

<execution_context>
@/Users/xavierpraveen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavierpraveen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check implemented-but-unchecked requirements and update AUTH-06</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Make the following targeted edits to `.planning/REQUIREMENTS.md`:

**1. AUTH-06** — Change from `[ ]` to `[x]` and add inline implementation note:
```markdown
- [x] **AUTH-06**: Middleware routes users to role-appropriate areas (employee / manager / leadership / admin) — v1 consolidation: manager, leadership, hr_admin, survey_analyst all route to /admin (see src/lib/constants/roles.ts ROLE_ROUTES)
```

**2. ANALYTICS-04** — Change `[ ]` to `[x]`:
```markdown
- [x] **ANALYTICS-04**: System displays confidence indicator when respondent count for a segment is below the privacy threshold
```

**3. DASH-01** — Change `[ ]` to `[x]`:
```markdown
- [x] **DASH-01**: Leadership dashboard shows: overall org health score, dimension scores, risk areas, participation rate, key qualitative themes, unresolved action items
```

**4. DASH-02** — Change `[ ]` to `[x]`:
```markdown
- [x] **DASH-02**: Leadership dashboard shows heatmap of scores by department
```

**5. DASH-03** — Change `[ ]` to `[x]`:
```markdown
- [x] **DASH-03**: Leadership dashboard shows trend lines across survey cycles per dimension
```

**6. DASH-04** — Change `[ ]` to `[x]`:
```markdown
- [x] **DASH-04**: Leadership dashboard supports filters by department, role, tenure band, and survey period
```

**7. ADMIN-02** — Change `[ ]` to `[x]`:
```markdown
- [x] **ADMIN-02**: Admin can manage questions: create, edit, reorder, delete, set type and targeting
```

**8. ADMIN-03** — Change `[ ]` to `[x]`:
```markdown
- [x] **ADMIN-03**: Admin can map questions to dimensions
```

**9. Update the coverage count** at the bottom of the file. The current count reads:
```
- Implemented [x]: 96/99
- Pending [ ]: 3 (AUTH-06, ANALYTICS-11, DASH-07 — closing in Phases 6–7)
```
Change to (AUTH-06 now closed, ANALYTICS-11 and DASH-07 still pending for Phase 7):
```
- Implemented [x]: 97/99
- Pending [ ]: 2 (ANALYTICS-11, DASH-07 — closing in Phase 7)
```

**10. Update the Traceability table** — change the AUTH-06 row from:
```
| AUTH-06 | Phase 6 | Pending |
```
to:
```
| AUTH-06 | Phase 6 | Complete |
```

**11. Update the footer note** at the bottom:
Change:
```
*Last updated: 2026-03-15 after roadmap creation*
```
to:
```
*Last updated: 2026-03-16 after Phase 6 gap closure*
```

Do not change any `[ ]` items that are genuinely unimplemented (ANALYTICS-11, DASH-07 remain `[ ]`).
  </action>
  <verify>
    <automated>grep -c "^\- \[ \]" /Users/xavierpraveen/Documents/Projects/surveyer/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>The grep count returns exactly 2 (only ANALYTICS-11 and DASH-07 remain unchecked). All other v1 requirements are `[x]`. AUTH-06 is `[x]` with inline consolidation note.</done>
</task>

</tasks>

<verification>
Confirm only ANALYTICS-11 and DASH-07 remain unchecked:
```bash
grep "^\- \[ \]" /Users/xavierpraveen/Documents/Projects/surveyer/.planning/REQUIREMENTS.md
```
Expected output:
```
- [ ] **ANALYTICS-11**: ...
- [ ] **DASH-07**: ...
```

Confirm AUTH-06 is checked:
```bash
grep "AUTH-06" /Users/xavierpraveen/Documents/Projects/surveyer/.planning/REQUIREMENTS.md
```
</verification>

<success_criteria>
- Exactly 2 unchecked `[ ]` requirements remain: ANALYTICS-11 and DASH-07
- AUTH-06 is `[x]` with inline v1 consolidation note
- Coverage count reads `97/99` implemented
- Traceability table shows AUTH-06 as Complete / Phase 6
- DASH-01–04, ANALYTICS-04, ADMIN-02, ADMIN-03 are all `[x]`
</success_criteria>

<output>
After completion, create `.planning/phases/06-critical-bug-fixes/06-03-SUMMARY.md`
</output>
