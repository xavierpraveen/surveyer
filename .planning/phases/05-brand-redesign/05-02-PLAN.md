---
phase: 05-brand-redesign
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/components/admin/SurveyStatusBanner.tsx
  - src/components/admin/SurveyList.tsx
  - src/components/admin/SettingsTabs.tsx
  - src/components/admin/ActionItemForm.tsx
  - src/components/admin/ActionUpdateTimeline.tsx
  - src/components/admin/CyclesTab.tsx
  - src/components/admin/EmployeeImportTab.tsx
  - src/components/admin/ParticipationMonitorTab.tsx
  - src/components/admin/PrivacySettingsTab.tsx
  - src/components/admin/PublishConfirmModal.tsx
  - src/components/admin/PublishResultsButton.tsx
  - src/components/admin/QuestionEditor.tsx
  - src/components/admin/SectionSidebar.tsx
  - src/components/admin/TaggingWorkspace.tsx
autonomous: true
requirements:
  - BRAND-03
  - BRAND-04
  - BRAND-05
must_haves:
  truths:
    - "All buttons in admin components use one of four spec-defined variants (Primary/Secondary/Ghost/Danger) — no ad-hoc bg-blue-500, bg-green-600, bg-gray-* button classes"
    - "All status badges in SurveyList and other admin components use spec badge token pairs (bg-*-muted + text-*-text)"
    - "All form inputs in admin components use the spec input pattern with focus:ring-indigo-200"
    - "SurveyStatusBanner uses spec-defined STATUS_STYLE token map (border-fg-subtle/bg-surface-2, border-warning/bg-warning-muted, border-success/bg-success-muted, border-fg-muted/bg-surface-2)"
    - "All admin components use token-based text colors (text-fg, text-fg-muted, text-fg-subtle) — no text-gray-*, text-slate-*"
  artifacts:
    - path: "src/components/admin/SurveyStatusBanner.tsx"
      provides: "Restyled banner with spec STATUS_STYLE map + spec button classes"
    - path: "src/components/admin/SurveyList.tsx"
      provides: "Restyled list with spec badge classes + spec button classes"
    - path: "src/components/admin/QuestionEditor.tsx"
      provides: "Restyled question editor with spec form inputs"
  key_links:
    - from: "src/components/admin/SurveyStatusBanner.tsx"
      to: "design tokens"
      via: "STATUS_STYLE map using border-fg-subtle, border-warning, border-success, border-fg-muted"
      pattern: "border-fg-subtle|border-warning|border-success|border-fg-muted"
    - from: "src/components/admin/SurveyList.tsx"
      to: "design tokens"
      via: "STATUS_BADGE map using bg-success-muted, bg-warning-muted, bg-surface-2, text-success-text"
      pattern: "bg-success-muted|bg-warning-muted|bg-surface-2"
---

<objective>
Restyle all 14 admin components to use the new design token system. Replace ad-hoc gray/green/blue/yellow Tailwind classes with semantic token classes. No functional changes.

Purpose: Admin users (the platform's primary power users) experience a polished, on-brand interface for all survey management, action tracking, settings, and tagging workflows.
Output: 14 restyled admin component files. All buttons, badges, inputs, cards, and status indicators use spec-defined token classes.
</objective>

<execution_context>
@/Users/xavierpraveen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavierpraveen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-brand-redesign/05-CONTEXT.md
@docs/superpowers/specs/2026-03-16-brand-redesign-design.md
@.planning/phases/05-brand-redesign/05-01-SUMMARY.md

<interfaces>
<!-- Design token classes — use ONLY these in restyled components. No ad-hoc Tailwind palette classes for semantic meaning. -->

BUTTON VARIANTS (exact strings — do not modify):
- Primary:   bg-brand hover:bg-brand-hover text-white font-semibold text-sm px-3.5 py-2 rounded-md transition-colors duration-150 disabled:opacity-50 disabled:cursor-not-allowed
- Secondary: bg-surface-2 hover:bg-border border border-border text-fg-muted font-medium text-sm px-3.5 py-2 rounded-md transition-colors duration-150 disabled:opacity-50 disabled:cursor-not-allowed
- Ghost:     bg-transparent hover:bg-brand-muted text-brand font-medium text-sm px-3.5 py-2 rounded-md transition-colors duration-150
- Danger:    bg-error hover:bg-error-text text-white font-semibold text-sm px-3.5 py-2 rounded-md transition-colors duration-150 disabled:opacity-50

BADGE BASE: inline-flex items-center rounded-full px-2 py-0.5 text-xs font-semibold
BADGE VARIANTS:
- Open:        bg-success-muted text-success-text
- Draft:       bg-surface-2 text-fg-muted
- Scheduled:   bg-warning-muted text-warning-text
- Closed:      bg-surface-2 text-fg-subtle
- Planned:     bg-brand-muted text-brand-text
- In Progress: bg-warning-muted text-warning-text
- Completed:   bg-success-muted text-success-text
- High:        bg-error-muted text-error-text
- Medium:      bg-warning-muted text-warning-text
- Low:         bg-surface-2 text-fg-muted

FORM INPUT: border border-border rounded-md bg-surface px-3 py-2 text-sm text-fg placeholder:text-fg-subtle focus:outline-none focus:ring-2 focus:ring-indigo-200 focus:border-border-focus
FORM INPUT ERROR STATE: add border-error focus:ring-red-200

CARD: bg-surface border border-border rounded-lg shadow-sm
CARD INTERACTIVE: add hover:border-indigo-300 hover:shadow-md transition-all duration-150

STATUS BANNER (SurveyStatusBanner):
const STATUS_STYLE = {
  draft:     'border-fg-subtle bg-surface-2',
  scheduled: 'border-warning bg-warning-muted',
  open:      'border-success bg-success-muted',
  closed:    'border-fg-muted bg-surface-2',
}

TEXT COLORS: text-fg (headings), text-fg-muted (secondary), text-fg-subtle (tertiary/placeholder)
HEADING SIZES: text-2xl font-extrabold tracking-snug, text-xl font-bold tracking-snug, text-base font-bold tracking-tight, text-sm font-semibold
SECTION DIVIDERS: border-border (not border-gray-200)
ACCESSIBILITY: Add focus-visible:ring-2 focus-visible:ring-indigo-300 focus-visible:outline-none to all interactive elements
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle SurveyStatusBanner, SurveyList, PublishResultsButton, PublishConfirmModal, SettingsTabs</name>
  <files>
    src/components/admin/SurveyStatusBanner.tsx,
    src/components/admin/SurveyList.tsx,
    src/components/admin/PublishResultsButton.tsx,
    src/components/admin/PublishConfirmModal.tsx,
    src/components/admin/SettingsTabs.tsx
  </files>
  <action>
Restyle these 5 components. Preserve all logic, state, imports, and JSX structure — change only className strings.

**SurveyStatusBanner.tsx:**
- Replace STATUS_STYLE map values with spec banner tokens (draft: 'border-fg-subtle bg-surface-2', scheduled: 'border-warning bg-warning-muted', open: 'border-success bg-success-muted', closed: 'border-fg-muted bg-surface-2')
- Replace all button classNames with spec variants: Schedule button → Secondary, Open Now → Primary, Close Survey → Danger, Compute Results → Primary
- Replace text-gray-500 → text-fg-muted, text-gray-900 → text-fg, text-gray-600 → text-fg-muted
- Replace inline form inputs with spec form input class string
- Replace border-gray-200 → border-border, bg-white → bg-surface
- "Status" label: change text-[10px] font-semibold uppercase tracking-[0.07em] text-fg-subtle (category label pattern)

**SurveyList.tsx:**
- Replace STATUS_BADGE map: draft → 'bg-surface-2 text-fg-muted', scheduled → 'bg-warning-muted text-warning-text', open → 'bg-success-muted text-success-text', closed → 'bg-surface-2 text-fg-subtle'
- Add badge base to each badge render: `inline-flex items-center rounded-full px-2 py-0.5 text-xs font-semibold`
- Survey title heading → text-base font-bold tracking-tight text-fg
- "New Survey" or primary action button → Primary variant
- Duplicate button → Ghost variant
- Link to survey detail → text-brand-text hover:underline (not text-blue-*)
- Card wrapper: bg-surface border border-border rounded-lg shadow-sm hover:border-indigo-300 hover:shadow-md transition-all duration-150
- Error message: text-xs text-error-text

**PublishResultsButton.tsx:**
- Main "Publish Results" button → Primary variant
- Ensure disabled state uses disabled:opacity-50

**PublishConfirmModal.tsx:**
- Modal backdrop: bg-fg/50 (or bg-black/50 — concrete class for opacity)
- Modal panel: bg-surface border border-border rounded-lg shadow-md p-6 max-w-md w-full
- Confirm button → Primary variant
- Cancel button → Secondary variant
- Heading: text-base font-bold tracking-tight text-fg
- Body text: text-sm text-fg-muted

**SettingsTabs.tsx:**
- Tab buttons: active tab → text-brand font-semibold border-b-2 border-brand; inactive → text-fg-muted font-medium hover:text-fg transition-colors
- Tab bar border: border-b border-border
  </action>
  <verify>
    <automated>cd /Users/xavierpraveen/Documents/Projects/surveyer && npx tsc --noEmit 2>&1 | grep -E "admin/(SurveyStatusBanner|SurveyList|PublishResultsButton|PublishConfirmModal|SettingsTabs)" | head -20</automated>
  </verify>
  <done>SurveyStatusBanner STATUS_STYLE uses spec border/bg tokens. SurveyList STATUS_BADGE uses spec badge token pairs. All buttons use spec variants. No ad-hoc bg-gray-*, bg-green-*, bg-blue-*, bg-yellow-* on buttons or badges in these 5 files. TypeScript compiles without errors on these files.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle ActionItemForm, ActionUpdateTimeline, CyclesTab, EmployeeImportTab, ParticipationMonitorTab, PrivacySettingsTab, QuestionEditor, SectionSidebar, TaggingWorkspace</name>
  <files>
    src/components/admin/ActionItemForm.tsx,
    src/components/admin/ActionUpdateTimeline.tsx,
    src/components/admin/CyclesTab.tsx,
    src/components/admin/EmployeeImportTab.tsx,
    src/components/admin/ParticipationMonitorTab.tsx,
    src/components/admin/PrivacySettingsTab.tsx,
    src/components/admin/QuestionEditor.tsx,
    src/components/admin/SectionSidebar.tsx,
    src/components/admin/TaggingWorkspace.tsx
  </files>
  <action>
Restyle these 9 components. Preserve all logic, state, imports, and JSX structure — change only className strings.

Apply these rules uniformly across all 9 files:

**All buttons:**
- Primary actions (Save, Submit, Add, Create, Import, Confirm, Generate): Primary variant
- Secondary/cancel actions (Cancel, Back, Reset): Secondary variant
- Destructive actions (Delete, Remove, Archive): Danger variant
- Subtle/navigation actions (Edit, View, Toggle): Ghost variant

**All form inputs (input, textarea, select):**
- Apply spec input class string: `border border-border rounded-md bg-surface px-3 py-2 text-sm text-fg placeholder:text-fg-subtle focus:outline-none focus:ring-2 focus:ring-indigo-200 focus:border-border-focus w-full`
- For error state: add `border-error focus:ring-red-200`
- All `<label>` elements: `block text-sm font-semibold text-fg mb-1`

**All cards/panels:**
- Card wrappers: `bg-surface border border-border rounded-lg shadow-sm`
- Card padding: `p-5` standard

**Text colors:**
- Primary text/headings: text-fg
- Secondary/supporting: text-fg-muted
- Tertiary/timestamps: text-fg-subtle
- Error messages: text-error-text (with role="alert" if announcing errors)
- Success messages: text-success-text

**Headings:**
- Section headings: text-base font-bold tracking-tight text-fg
- Sub-headings / labels: text-sm font-semibold text-fg

**Borders/dividers:** border-border (replacing border-gray-200, border-slate-200, etc.)

**Backgrounds:** bg-surface (white), bg-surface-2 (light gray rows/alt backgrounds), bg-bg (page bg)

**Specific notes per component:**
- ActionUpdateTimeline: timeline dots → bg-brand-muted border border-brand; timestamps → text-fg-subtle text-xs
- CyclesTab: archive badge → bg-surface-2 text-fg-subtle; active cycle highlight → bg-brand-muted
- ParticipationMonitorTab: progress fill → bg-gradient-to-r from-brand to-accent h-1.5 rounded-full; track → bg-brand-muted h-1.5 rounded-full
- TaggingWorkspace: theme tag pills → bg-brand-muted text-brand-text text-xs font-semibold rounded-full px-2 py-0.5; selected state → bg-brand text-white
- SectionSidebar: active section → bg-brand-muted text-brand font-semibold; inactive → text-fg-muted hover:bg-surface-2; add section button → Ghost variant
  </action>
  <verify>
    <automated>cd /Users/xavierpraveen/Documents/Projects/surveyer && npx tsc --noEmit 2>&1 | grep "components/admin" | head -20</automated>
  </verify>
  <done>All 9 components use spec button variants, spec form input class, spec text/surface token colors. No ad-hoc gray/green/blue/yellow classes remain on interactive or semantic elements. TypeScript compiles without errors on all 14 admin component files.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsc --noEmit` — zero errors in components/admin/
2. Search for ad-hoc color classes in admin components: `grep -r "bg-gray-\|bg-green-\|bg-blue-\|bg-yellow-\|text-gray-\|text-green-\|text-blue-\|text-yellow-" src/components/admin/` — should return zero results (only token classes should remain)
3. Confirm SurveyStatusBanner STATUS_STYLE uses 'border-fg-subtle', 'border-warning', 'border-success', 'border-fg-muted'
4. Confirm SurveyList STATUS_BADGE uses 'bg-success-muted', 'bg-warning-muted', 'bg-surface-2', 'text-success-text'
</verification>

<success_criteria>
- All 14 admin components compile without TypeScript errors
- All button variants match one of the four spec strings exactly
- SurveyStatusBanner STATUS_STYLE maps to spec banner token pairs
- SurveyList STATUS_BADGE maps to spec badge token pairs
- All form inputs use spec input class pattern
- No ad-hoc palette color classes (bg-blue-500, bg-green-600, text-gray-700, etc.) remain on semantic elements
</success_criteria>

<output>
After completion, create `.planning/phases/05-brand-redesign/05-02-SUMMARY.md`
</output>
