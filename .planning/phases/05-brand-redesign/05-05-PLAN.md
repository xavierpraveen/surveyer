---
phase: 05-brand-redesign
plan: "05"
type: execute
wave: 4
depends_on:
  - "05-02"
  - "05-03"
  - "05-04"
files_modified:
  - src/app/(admin)/admin/page.tsx
  - src/app/(admin)/admin/surveys/page.tsx
  - src/app/(admin)/admin/surveys/[id]/page.tsx
  - src/app/(admin)/admin/surveys/[id]/tags/page.tsx
  - src/app/(admin)/admin/surveys/new/page.tsx
  - src/app/(admin)/admin/actions/page.tsx
  - src/app/(admin)/admin/actions/[id]/page.tsx
  - src/app/(admin)/admin/settings/page.tsx
  - src/app/(employee)/dashboard/page.tsx
  - src/app/(employee)/surveys/[id]/page.tsx
  - src/app/(employee)/surveys/[id]/confirmation/page.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/magic-link/page.tsx
  - src/app/results/page.tsx
  - src/app/survey/public/[id]/page.tsx
  - src/app/survey/public/[id]/confirmation/page.tsx
autonomous: false
requirements:
  - BRAND-06
  - BRAND-07
must_haves:
  truths:
    - "Admin pages use max-w-6xl mx-auto p-8 layout with bg-bg page background"
    - "Employee/survey pages use max-w-3xl mx-auto p-8 layout"
    - "Auth pages (login, magic-link) use centered card layout with bg-bg"
    - "All page headings follow spec typography scale (text-2xl font-extrabold tracking-snug for heroes)"
    - "No min-h-screen bg-gray-50 or similar ad-hoc page background classes remain"
    - "The app loads without console errors about undefined Tailwind tokens"
  artifacts:
    - path: "src/app/(admin)/admin/page.tsx"
      provides: "Admin landing page with token-based layout and nav cards"
    - path: "src/app/(employee)/dashboard/page.tsx"
      provides: "Employee dashboard with token-based survey cards"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Auth page with centered card layout"
  key_links:
    - from: "src/app/(admin)/admin/page.tsx"
      to: "design tokens"
      via: "bg-bg page background, max-w-6xl layout, token heading classes"
      pattern: "bg-bg|max-w-6xl"
    - from: "src/app/(employee)/dashboard/page.tsx"
      to: "design tokens"
      via: "max-w-3xl layout, survey card token classes"
      pattern: "max-w-3xl|bg-surface"
---

<objective>
Restyle all 16 page files across admin, employee, auth, results, and public survey routes. Apply spec page layouts, typography scale, and token-based backgrounds. These are the final files in the visual identity update.

Purpose: Page-level shell styling ties all components together. Without correct page backgrounds (bg-bg), max-widths, and heading typography, individual component restyling appears inconsistent.
Output: 16 restyled page files. No functional changes — layout structure (max-w, padding, headings, card wrappers) updated to spec.
</objective>

<execution_context>
@/Users/xavierpraveen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavierpraveen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-brand-redesign/05-CONTEXT.md
@docs/superpowers/specs/2026-03-16-brand-redesign-design.md
@.planning/phases/05-brand-redesign/05-01-SUMMARY.md
@.planning/phases/05-brand-redesign/05-02-SUMMARY.md
@.planning/phases/05-brand-redesign/05-03-SUMMARY.md
@.planning/phases/05-brand-redesign/05-04-SUMMARY.md

<interfaces>
<!-- Page-level layout patterns from spec Section 5 -->

ADMIN PAGES (max-w-6xl):
- Page shell: <div className="max-w-6xl mx-auto p-8">
- (Note: min-h-screen bg-bg is set by (admin)/layout.tsx main element — do NOT repeat in pages)
- Page hero heading: text-2xl font-extrabold tracking-snug text-fg
- Page subtitle: text-sm text-fg-muted mt-1
- Section heading: text-xl font-bold tracking-snug text-fg mt-8 mb-4 (first section: mt-0 mb-4)
- Card grid: grid grid-cols-1 gap-4 (or md:grid-cols-2 lg:grid-cols-3 where appropriate)
- Nav/quick-access cards (admin landing): bg-surface border border-border rounded-lg shadow-sm p-5 hover:border-indigo-300 hover:shadow-md transition-all duration-150 cursor-pointer

EMPLOYEE PAGES (max-w-3xl):
- Page shell: <div className="max-w-3xl mx-auto p-8">
- Page hero heading: text-2xl font-extrabold tracking-snug text-fg
- Survey cards: bg-surface border border-border rounded-lg shadow-sm p-5 hover:border-indigo-300 hover:shadow-md transition-all duration-150
- Empty state: text-sm text-fg-muted text-center py-12

AUTH PAGES (centered, narrow):
- Page shell: <div className="min-h-screen bg-bg flex items-center justify-center p-4">
  (Auth pages do NOT have TopNav and must provide their own background + centering)
- Card: bg-surface border border-border rounded-lg shadow-md p-8 w-full max-w-sm
- Logo/brand mark at top of card: indigo circle + "Surveyer" (same as TopNav logo, reuse pattern)
- Heading: text-xl font-bold tracking-snug text-fg text-center mb-6
- Input fields: spec form input class
- Submit button: Primary variant, w-full
- Link text: text-brand-text hover:underline text-sm

RESULTS PAGE (max-w-6xl, no TopNav — standalone):
- Page shell: <div className="min-h-screen bg-bg"><div className="max-w-6xl mx-auto p-8">
  (Results page must provide its own bg-bg since it's outside (admin)/(employee) layouts)
- Page heading: text-2xl font-extrabold tracking-snug text-fg
- Analytics components are already restyled — just ensure page shell is correct

PUBLIC SURVEY PAGE (max-w-3xl, no TopNav):
- Page shell: <div className="min-h-screen bg-bg"><div className="max-w-3xl mx-auto p-8">
- SurveyWizard is already restyled — page shell only

REPLACE these old patterns wherever found:
- min-h-screen bg-gray-50 → (remove: provided by layout; or for standalone pages: min-h-screen bg-bg)
- max-w-4xl → max-w-6xl (admin) or max-w-3xl (employee/survey)
- text-gray-900 → text-fg
- text-gray-500, text-gray-600 → text-fg-muted
- text-slate-500 → text-fg-muted
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle 8 admin page files</name>
  <files>
    src/app/(admin)/admin/page.tsx,
    src/app/(admin)/admin/surveys/page.tsx,
    src/app/(admin)/admin/surveys/[id]/page.tsx,
    src/app/(admin)/admin/surveys/[id]/tags/page.tsx,
    src/app/(admin)/admin/surveys/new/page.tsx,
    src/app/(admin)/admin/actions/page.tsx,
    src/app/(admin)/admin/actions/[id]/page.tsx,
    src/app/(admin)/admin/settings/page.tsx
  </files>
  <action>
Read each file before editing. Preserve all data fetching (RSC async functions, Supabase calls, Server Actions), component imports, and conditional rendering. Change only layout/wrapper className strings and heading classes.

Apply to ALL 8 admin pages:
- Outermost div: `max-w-6xl mx-auto p-8` (remove any min-h-screen bg-gray-50 — that's handled by layout.tsx)
- Page hero heading (h1): `text-2xl font-extrabold tracking-snug text-fg`
- Page subtitle/description: `text-sm text-fg-muted mt-1`
- Section headings (h2): `text-xl font-bold tracking-snug text-fg mb-4`
- Sub-section headings (h3): `text-base font-bold tracking-tight text-fg mb-3`

Specific per-page notes:
- **admin/page.tsx** (admin landing): Nav card grid — cards use `bg-surface border border-border rounded-lg shadow-sm p-5 hover:border-indigo-300 hover:shadow-md transition-all duration-150`. Card title: `text-base font-bold tracking-tight text-fg`. Card description: `text-sm text-fg-muted mt-1`. Wrap in `grid grid-cols-1 md:grid-cols-3 gap-4`.
- **admin/surveys/page.tsx**: Page heading "Surveys" + "New Survey" button → Primary variant. If there is an inline survey status filter tab bar, restyle with border-b border-border and active/inactive tab classes matching SettingsTabs pattern.
- **admin/surveys/[id]/page.tsx**: SurveyStatusBanner already restyled (plan 02) — just fix page wrapper classes. Survey detail card: bg-surface border border-border rounded-lg shadow-sm p-5.
- **admin/surveys/[id]/tags/page.tsx**: Page heading + TaggingWorkspace already restyled. Fix wrapper.
- **admin/surveys/new/page.tsx**: Fix page wrapper. New survey form: bg-surface border border-border rounded-lg shadow-sm p-5.
- **admin/actions/page.tsx**: Status filter tabs → same tab pattern (border-b border-border, active: text-brand border-b-2 border-brand). Actions table: bg-surface border border-border rounded-lg shadow-sm overflow-hidden. Table header: text-xs font-semibold text-fg-subtle uppercase tracking-[0.07em].
- **admin/actions/[id]/page.tsx**: Detail card wrapper. "Back" link: Ghost variant or `text-brand-text hover:underline text-sm flex items-center gap-1`.
- **admin/settings/page.tsx**: SettingsTabs wrapper. Fix page heading.
  </action>
  <verify>
    <automated>cd /Users/xavierpraveen/Documents/Projects/surveyer && npx tsc --noEmit 2>&1 | grep "app/(admin)" | head -20</automated>
  </verify>
  <done>All 8 admin pages use max-w-6xl mx-auto p-8 outer wrapper. Page hero headings use text-2xl font-extrabold tracking-snug text-fg. Admin landing nav cards use spec interactive card classes. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle employee, auth, results, and public survey pages</name>
  <files>
    src/app/(employee)/dashboard/page.tsx,
    src/app/(employee)/surveys/[id]/page.tsx,
    src/app/(employee)/surveys/[id]/confirmation/page.tsx,
    src/app/(auth)/login/page.tsx,
    src/app/(auth)/magic-link/page.tsx,
    src/app/results/page.tsx,
    src/app/survey/public/[id]/page.tsx,
    src/app/survey/public/[id]/confirmation/page.tsx
  </files>
  <action>
Read each file before editing. Preserve all data fetching, auth logic, survey/confirmation state, and Server Action calls. Change only layout/wrapper className strings.

**Employee pages (max-w-3xl, bg handled by layout.tsx):**
- dashboard/page.tsx: `max-w-3xl mx-auto p-8`. Page heading: `text-2xl font-extrabold tracking-snug text-fg`. Survey card grid: `grid grid-cols-1 gap-4`. Each survey card: `bg-surface border border-border rounded-lg shadow-sm p-5 hover:border-indigo-300 hover:shadow-md transition-all duration-150`. Empty state: `text-sm text-fg-muted text-center py-12`. "Take Survey" link/button: Primary variant.
- surveys/[id]/page.tsx: `max-w-3xl mx-auto p-8` wrapper. SurveyWizard is already restyled — fix page wrapper only.
- surveys/[id]/confirmation/page.tsx: `max-w-3xl mx-auto p-8`. Confirmation card: `bg-surface border border-border rounded-lg shadow-sm p-8 text-center`. Success icon/checkmark: `text-success text-4xl mb-4` (or SVG). Heading: `text-xl font-bold tracking-snug text-fg`. Body: `text-sm text-fg-muted mt-2`. Return link: Primary variant.

**Auth pages (standalone — must provide own bg-bg + centering):**
- login/page.tsx: Replace page shell with `min-h-screen bg-bg flex items-center justify-center p-4`. Auth card: `bg-surface border border-border rounded-lg shadow-md p-8 w-full max-w-sm`. Logo mark at top: indigo `w-8 h-8 rounded-md bg-gradient-to-br from-brand to-accent` + "Surveyer" `text-fg font-extrabold text-sm tracking-tight` in a `flex items-center gap-2 justify-center mb-6`. Heading: `text-xl font-bold tracking-snug text-fg text-center mb-6`. All inputs: spec input class. Submit button: Primary variant w-full. Error text: `text-xs text-error-text` with role="alert". Links (magic link, etc.): `text-brand-text hover:underline text-sm`.
- magic-link/page.tsx: Same auth page shell. Typically shows a confirmation message — `text-sm text-fg-muted text-center`. Success state heading: `text-xl font-bold tracking-snug text-fg text-center`. "Back to login" link: Ghost variant or text-brand-text.

**Results page (standalone — must provide own bg-bg):**
- results/page.tsx: `min-h-screen bg-bg` outer shell. Inner: `max-w-6xl mx-auto p-8`. Page heading: `text-2xl font-extrabold tracking-snug text-fg`. Analytics components (KpiStrip, DimensionBarChart, etc.) already restyled in plan 04.

**Public survey pages (standalone — must provide own bg-bg):**
- survey/public/[id]/page.tsx: `min-h-screen bg-bg`. Inner: `max-w-3xl mx-auto p-8`. SurveyWizard already restyled.
- survey/public/[id]/confirmation/page.tsx: `min-h-screen bg-bg`. Inner: `max-w-3xl mx-auto p-8`. Same confirmation card pattern as employee confirmation.
  </action>
  <verify>
    <automated>cd /Users/xavierpraveen/Documents/Projects/surveyer && npx tsc --noEmit 2>&1 | grep -E "app/(employee|auth)|app/results|app/survey/public" | head -20</automated>
  </verify>
  <done>All 8 pages compile without TypeScript errors. Auth pages have min-h-screen bg-bg shell. Employee pages use max-w-3xl. Results and public survey pages have standalone bg-bg. Confirmation pages have centered card layout.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 5 Brand Redesign — all 30 files restyled. Design token system (globals.css, tailwind.config.ts), Inter typeface, TopNav on admin/employee pages, all button/badge/card/input/Likert patterns updated across admin, survey, analytics, and page files.</what-built>
  <how-to-verify>
1. Start the dev server: `npm run dev` from the project root
2. Sign in as admin and visit http://localhost:3000/admin — confirm TopNav visible, page uses bg-bg, cards use bg-surface border border-border
3. Navigate to /admin/surveys — confirm survey status badges use spec badge colors (Open = emerald tint, Draft = gray tint)
4. Open a survey detail page — confirm SurveyStatusBanner uses correct border colors per status
5. Visit http://localhost:3000/dashboard as an employee — confirm survey cards have hover:border-indigo-300 effect
6. Take a survey: navigate to /surveys/[id] — confirm progress bar shows indigo→violet gradient, Likert buttons show border-brand bg-brand-muted when selected
7. Visit http://localhost:3000/login — confirm centered auth card with indigo logo mark, white card on slate-50 background
8. Visit http://localhost:3000/results — confirm analytics components have token-based card wrappers
9. Check browser console — zero errors about undefined Tailwind classes or missing CSS variables
10. Run `npm run build` — zero build errors
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any visual/functional issues found</resume-signal>
</task>

</tasks>

<verification>
After automated tasks complete (before checkpoint):
1. `npx tsc --noEmit` from project root — zero errors across all 30 restyled files
2. `grep -r "bg-gray-50\|min-h-screen bg-gray-" src/app/\(admin\)/ src/app/\(employee\)/` — zero results (layout.tsx handles bg)
3. `grep -r "bg-gray-50\|bg-gray-100\|text-gray-900\|text-gray-500\|text-slate-500" src/app/ src/components/` — zero (or only in legacy leadership/manager routes)
4. `npm run build` — successful build
</verification>

<success_criteria>
- All 16 page files use spec layout patterns (max-w-6xl or max-w-3xl, p-8, token colors)
- Auth pages provide standalone bg-bg centering
- Results and public survey pages provide standalone bg-bg
- No ad-hoc bg-gray-50, text-gray-900, text-slate-500 in restyled files
- Zero TypeScript errors
- Zero build errors
- Human visual verification: TopNav appears, brand colors visible, survey Likert scale interactive states work
</success_criteria>

<output>
After completion, create `.planning/phases/05-brand-redesign/05-05-SUMMARY.md`
</output>
