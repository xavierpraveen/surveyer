---
phase: 05-brand-redesign
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/components/survey/SurveyWizard.tsx
  - src/components/survey/QuestionRenderer.tsx
  - src/components/survey/SurveyProgressBar.tsx
  - src/components/survey/ConditionalQuestion.tsx
autonomous: true
requirements:
  - BRAND-05
must_haves:
  truths:
    - "Likert scale buttons use spec-defined unselected/selected/hover states (border-brand, bg-brand-muted, text-brand, font-bold)"
    - "Progress bar track uses bg-brand-muted h-1.5 rounded-full and fill uses bg-gradient-to-r from-brand to-accent"
    - "Navigation buttons in SurveyWizard (Next, Previous, Submit) use spec button variants"
    - "All form inputs in survey components use spec input pattern"
    - "ConditionalQuestion reveal animation CSS max-h-0 overflow-hidden pattern preserved (functional)"
  artifacts:
    - path: "src/components/survey/SurveyProgressBar.tsx"
      provides: "Spec-defined gradient progress bar"
    - path: "src/components/survey/QuestionRenderer.tsx"
      provides: "Spec-defined Likert scale button states"
    - path: "src/components/survey/SurveyWizard.tsx"
      provides: "Restyled wizard with spec buttons and layout"
    - path: "src/components/survey/ConditionalQuestion.tsx"
      provides: "Restyled conditional wrapper preserving reveal logic"
  key_links:
    - from: "src/components/survey/SurveyWizard.tsx"
      to: "src/components/survey/SurveyProgressBar.tsx"
      via: "renders SurveyProgressBar with progress prop"
      pattern: "<SurveyProgressBar"
    - from: "src/components/survey/SurveyWizard.tsx"
      to: "src/components/survey/QuestionRenderer.tsx"
      via: "renders QuestionRenderer for each question"
      pattern: "<QuestionRenderer"
---

<objective>
Restyle the four survey components: SurveyWizard, QuestionRenderer, SurveyProgressBar, and ConditionalQuestion. Apply spec Likert scale states, gradient progress bar, and spec button/input classes.

Purpose: Employees taking surveys experience the new brand identity. The Likert scale and progress bar are the most visible survey UI elements — they must match the spec exactly.
Output: 4 restyled survey component files. Functional survey flow preserved; only className strings change.
</objective>

<execution_context>
@/Users/xavierpraveen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavierpraveen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-brand-redesign/05-CONTEXT.md
@docs/superpowers/specs/2026-03-16-brand-redesign-design.md
@.planning/phases/05-brand-redesign/05-01-SUMMARY.md

<interfaces>
<!-- Design token rules for survey components -->

LIKERT SCALE BUTTON STATES (exact strings — apply via ternary on selected prop):
- Unselected: border border-border bg-surface text-fg-muted rounded-md py-2 text-sm hover:border-indigo-300 hover:bg-brand-muted transition-colors duration-150 focus-visible:ring-2 focus-visible:ring-indigo-300 focus-visible:outline-none
- Selected:   border-2 border-brand bg-brand-muted text-brand font-bold rounded-md py-2 text-sm focus-visible:ring-2 focus-visible:ring-indigo-300 focus-visible:outline-none

PROGRESS BAR:
- Outer track wrapper: relative bg-brand-muted h-1.5 rounded-full overflow-hidden
- Fill div: absolute inset-y-0 left-0 bg-gradient-to-r from-brand to-accent h-1.5 rounded-full transition-all duration-300

BUTTON VARIANTS:
- Primary:   bg-brand hover:bg-brand-hover text-white font-semibold text-sm px-3.5 py-2 rounded-md transition-colors duration-150 disabled:opacity-50 disabled:cursor-not-allowed
- Secondary: bg-surface-2 hover:bg-border border border-border text-fg-muted font-medium text-sm px-3.5 py-2 rounded-md transition-colors duration-150 disabled:opacity-50 disabled:cursor-not-allowed
- Ghost:     bg-transparent hover:bg-brand-muted text-brand font-medium text-sm px-3.5 py-2 rounded-md transition-colors duration-150

FORM INPUT: border border-border rounded-md bg-surface px-3 py-2 text-sm text-fg placeholder:text-fg-subtle focus:outline-none focus:ring-2 focus:ring-indigo-200 focus:border-border-focus w-full

AUTOSAVE INDICATOR: text-xs text-fg-subtle (idle/saved), text-xs text-fg-muted animate-pulse (saving), text-xs text-error-text (error)

SURVEY CONTAINER MAX WIDTH: max-w-3xl mx-auto (not max-w-6xl — survey flow is narrower)
PAGE PADDING: p-8 desktop (applied by page, not wizard itself)

ConditionalQuestion: CRITICAL — preserve the CSS max-h-0 overflow-hidden transition logic for reveal animation. Only restyle the container/wrapper classes, not the reveal mechanism.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle SurveyProgressBar and QuestionRenderer</name>
  <files>src/components/survey/SurveyProgressBar.tsx, src/components/survey/QuestionRenderer.tsx</files>
  <action>
**SurveyProgressBar.tsx** — Read the current file first. Replace the progress bar track and fill classes with the spec pattern:
- Track: `bg-brand-muted h-1.5 rounded-full overflow-hidden` (relative wrapper)
- Fill: `bg-gradient-to-r from-brand to-accent h-1.5 rounded-full transition-all duration-300` with width set via inline style to `${progress}%`
- If a percentage label exists: `text-xs text-fg-muted tabular-nums`
- Preserve the progress prop interface and percentage calculation logic

**QuestionRenderer.tsx** — Read the current file first. Find all Likert/rating question button rendering. Apply spec states:
- Unselected state: `border border-border bg-surface text-fg-muted rounded-md py-2 text-sm hover:border-indigo-300 hover:bg-brand-muted transition-colors duration-150 focus-visible:ring-2 focus-visible:ring-indigo-300 focus-visible:outline-none`
- Selected state: `border-2 border-brand bg-brand-muted text-brand font-bold rounded-md py-2 text-sm focus-visible:ring-2 focus-visible:ring-indigo-300 focus-visible:outline-none`
- Use a ternary: `selected ? selectedClasses : unselectedClasses`
- For text question inputs (textarea, input): apply spec form input classes
- For scale labels (e.g., "Strongly Disagree" / "Strongly Agree"): `text-xs text-fg-subtle`
- Question text (the question prompt): `text-sm font-semibold text-fg mb-3`
  </action>
  <verify>
    <automated>cd /Users/xavierpraveen/Documents/Projects/surveyer && npx tsc --noEmit 2>&1 | grep -E "survey/(SurveyProgressBar|QuestionRenderer)" | head -20</automated>
  </verify>
  <done>SurveyProgressBar renders gradient fill bar with bg-gradient-to-r from-brand to-accent. QuestionRenderer Likert buttons use spec selected/unselected token classes. TypeScript compiles without errors on both files.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle SurveyWizard and ConditionalQuestion</name>
  <files>src/components/survey/SurveyWizard.tsx, src/components/survey/ConditionalQuestion.tsx</files>
  <action>
**SurveyWizard.tsx** — Read the current file first. Apply spec restyling:
- Wrapper/container: no structural changes — only update className strings
- Section title (active section heading): `text-xl font-bold tracking-snug text-fg mb-6`
- Section navigation buttons: Previous → Secondary variant, Next → Primary variant, Submit → Primary variant
- "Submitting..." / loading states: preserve disabled:opacity-50 on Primary
- Save status indicator: idle → `text-xs text-fg-subtle`, saving → `text-xs text-fg-muted`, saved → `text-xs text-success-text`, error → `text-xs text-error-text`
- Error banner (if any): `bg-error-muted border border-error text-error-text text-sm px-4 py-3 rounded-md` with role="alert"
- Nav step indicators (section progress dots/steps): active → `bg-brand text-white`, completed → `bg-success text-white`, upcoming → `bg-surface-2 text-fg-muted`
- Survey title: `text-2xl font-extrabold tracking-snug text-fg`
- Preserve all state, hooks, autosave logic, section traversal, and conditional rendering logic unchanged

**ConditionalQuestion.tsx** — Read the current file first. CRITICAL CONSTRAINT: preserve the CSS max-h transition/overflow-hidden reveal mechanism exactly. The spec decision records: "ConditionalQuestion keeps children mounted using CSS max-h-0 overflow-hidden to avoid re-mounting inputs and losing state". Only change cosmetic wrapper classes:
- Outer wrapper div: no class change needed (the reveal mechanism handles visibility)
- If there is a question prompt/label inside ConditionalQuestion: apply `text-sm font-semibold text-fg mb-3`
- Preserve max-h transition classes: max-h-0 (hidden) / max-h-[500px] or similar (visible), overflow-hidden, transition-all duration-300
  </action>
  <verify>
    <automated>cd /Users/xavierpraveen/Documents/Projects/surveyer && npx tsc --noEmit 2>&1 | grep -E "survey/(SurveyWizard|ConditionalQuestion)" | head -20</automated>
  </verify>
  <done>SurveyWizard navigation buttons use Primary/Secondary spec variants. ConditionalQuestion max-h reveal logic preserved. TypeScript compiles without errors on all 4 survey component files.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsc --noEmit` — zero errors in components/survey/
2. Verify progress bar: `grep "from-brand to-accent" src/components/survey/SurveyProgressBar.tsx` — must exist
3. Verify Likert states: `grep "border-brand" src/components/survey/QuestionRenderer.tsx` — must exist for selected state
4. Verify ConditionalQuestion reveal preserved: `grep "max-h" src/components/survey/ConditionalQuestion.tsx` — must still exist
5. No ad-hoc palette classes on interactive elements: `grep -r "bg-blue-\|bg-green-\|bg-gray-" src/components/survey/` — zero results
</verification>

<success_criteria>
- SurveyProgressBar: gradient fill (from-brand to-accent), brand-muted track
- QuestionRenderer: Likert buttons use spec selected (border-brand bg-brand-muted text-brand font-bold) and unselected (border-border bg-surface text-fg-muted) token strings
- SurveyWizard: Next/Submit = Primary, Previous = Secondary; autosave indicator uses token text colors
- ConditionalQuestion: max-h reveal mechanism intact; only cosmetic wrapper classes updated
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-brand-redesign/05-03-SUMMARY.md`
</output>
