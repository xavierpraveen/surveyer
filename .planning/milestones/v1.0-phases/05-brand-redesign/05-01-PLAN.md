---
phase: 05-brand-redesign
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - tailwind.config.ts
  - src/app/layout.tsx
  - src/components/layout/TopNav.tsx
  - src/app/(admin)/layout.tsx
  - src/app/(employee)/layout.tsx
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
must_haves:
  truths:
    - "All 21 CSS custom properties exist under :root in globals.css"
    - "Tailwind config maps every token to var(--color-*) and extends fontFamily, letterSpacing, boxShadow, borderRadius, ringColor"
    - "Inter loads via next/font/google with variable --font-inter applied to <html>"
    - "TopNav renders on all (admin) and (employee) pages with logo, nav links, and user avatar"
    - "No TopNav on (auth), survey/public/, or results/ routes"
  artifacts:
    - path: "src/app/globals.css"
      provides: "21 semantic CSS custom properties + reduced-motion rule"
    - path: "tailwind.config.ts"
      provides: "Full design token Tailwind mapping"
    - path: "src/components/layout/TopNav.tsx"
      provides: "TopNav component with logo + nav + avatar"
    - path: "src/app/(admin)/layout.tsx"
      provides: "Admin route group layout with TopNav"
    - path: "src/app/(employee)/layout.tsx"
      provides: "Employee route group layout with TopNav"
  key_links:
    - from: "src/app/layout.tsx"
      to: "next/font/google"
      via: "Inter import with variable: '--font-inter'"
      pattern: "inter\\.variable"
    - from: "src/app/(admin)/layout.tsx"
      to: "src/components/layout/TopNav.tsx"
      via: "import and render"
      pattern: "<TopNav"
    - from: "tailwind.config.ts"
      to: "globals.css"
      via: "var(--color-*) references"
      pattern: "var\\(--color-"
---

<objective>
Lay the complete design system foundation: CSS custom properties, Tailwind token mapping, Inter typeface, TopNav component, and route group layouts for (admin) and (employee).

Purpose: Every downstream plan depends on these tokens and the TopNav layout wrapper. Nothing else can be restyled until this infrastructure is in place.
Output: 6 modified/created files. After this plan, `bg-brand`, `text-fg-muted`, `border-border` etc. are valid Tailwind classes everywhere in the app; TopNav appears on all admin and employee pages.
</objective>

<execution_context>
@/Users/xavierpraveen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavierpraveen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-brand-redesign/05-CONTEXT.md
@docs/superpowers/specs/2026-03-16-brand-redesign-design.md

<interfaces>
<!-- Existing files being modified — key signatures to preserve -->

From src/app/layout.tsx (current):
```tsx
import type { Metadata } from 'next'
import './globals.css'
export const metadata: Metadata = { title: 'Surveyer', description: '...' }
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
```

From tailwind.config.ts (current):
```ts
import type { Config } from 'tailwindcss'
const config: Config = { content: ['./src/**/*.{ts,tsx}'], theme: { extend: {} }, plugins: [] }
export default config
```

From src/app/globals.css (current):
```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

Auth context for TopNav (to get current user):
```ts
// Use createSupabaseServerClient from '@/lib/supabase/server' (async RSC function)
const supabase = await createSupabaseServerClient()
const { data: { user } } = await supabase.auth.getUser()
// user.email — use prefix before '@' as avatar initials fallback
// user.user_metadata?.full_name — first letter as avatar if available
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design token infrastructure (globals.css + tailwind.config.ts + layout.tsx)</name>
  <files>src/app/globals.css, tailwind.config.ts, src/app/layout.tsx</files>
  <action>
**globals.css** — Replace the existing file entirely. Keep the three @tailwind directives at the top. Add the 21 CSS custom properties under :root exactly as specified, followed by the reduced-motion media query:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --color-brand:          #6366F1;
  --color-brand-hover:    #4F46E5;
  --color-brand-muted:    #EEF2FF;
  --color-brand-text:     #4338CA;
  --color-accent:         #8B5CF6;
  --color-bg:             #F8FAFC;
  --color-surface:        #FFFFFF;
  --color-surface-2:      #F1F5F9;
  --color-fg:             #0F172A;
  --color-fg-muted:       #64748B;
  --color-fg-subtle:      #94A3B8;
  --color-border:         #E2E8F0;
  --color-border-focus:   #6366F1;
  --color-success:        #10B981;
  --color-success-muted:  #DCFCE7;
  --color-success-text:   #065F46;
  --color-warning:        #F59E0B;
  --color-warning-muted:  #FEF3C7;
  --color-warning-text:   #92400E;
  --color-error:          #EF4444;
  --color-error-muted:    #FEE2E2;
  --color-error-text:     #991B1B;
}

@media (prefers-reduced-motion: reduce) {
  * { transition: none !important; animation: none !important; }
}
```

Do NOT add an @import for Inter — Inter loads exclusively via next/font/google.

**tailwind.config.ts** — Replace entirely with the full config from spec Section 3. It must include: colors mapping all 21 tokens to var(--color-*), fontFamily.sans with Inter fallbacks, borderRadius DEFAULT = '6px', letterSpacing.snug/tight, boxShadow.sm/md overrides, ringColor.DEFAULT = 'var(--color-brand)'. Preserve `content: ['./src/**/*.{ts,tsx}']` and `plugins: []`.

**src/app/layout.tsx** — Add Inter from next/font/google. Import `Inter` with `subsets: ['latin'], display: 'swap', variable: '--font-inter'`. Apply `inter.variable` to the `<html>` className. Also add `antialiased` to `<body>` className. Preserve existing Metadata export unchanged.
  </action>
  <verify>
    <automated>cd /Users/xavierpraveen/Documents/Projects/surveyer && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>globals.css has all 21 custom properties and reduced-motion rule. tailwind.config.ts has full token mapping. layout.tsx imports Inter and applies inter.variable to html element. TypeScript compiles without errors on these files.</done>
</task>

<task type="auto">
  <name>Task 2: TopNav component + route group layouts</name>
  <files>src/components/layout/TopNav.tsx, src/app/(admin)/layout.tsx, src/app/(employee)/layout.tsx</files>
  <action>
**Create directory** `src/components/layout/` first (mkdir -p equivalent via file creation).

**src/components/layout/TopNav.tsx** — RSC (no 'use client'). Fetches current user via createSupabaseServerClient. Renders:

```tsx
import Link from 'next/link'
import { createSupabaseServerClient } from '@/lib/supabase/server'

export default async function TopNav() {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()

  // Compute initials: first letter of full_name, or first letter of email prefix, uppercased
  const name: string = user?.user_metadata?.full_name ?? user?.email ?? 'U'
  const initials = name.includes(' ')
    ? name.split(' ').slice(0, 2).map((p: string) => p[0]).join('').toUpperCase()
    : name.replace(/@.*/, '')[0]?.toUpperCase() ?? 'U'

  return (
    <nav className="bg-surface border-b border-border h-11 px-5 flex items-center justify-between">
      <div className="flex items-center gap-6">
        {/* Logo */}
        <Link href="/" className="flex items-center gap-2">
          <span
            className="w-[22px] h-[22px] rounded-md bg-gradient-to-br from-brand to-accent flex-shrink-0"
            aria-hidden="true"
          />
          <span className="text-fg font-extrabold text-sm tracking-tight">Surveyer</span>
        </Link>
        {/* Nav links — rendered as plain links; active detection is a future enhancement */}
        <div className="flex items-center gap-4">
          <Link href="/admin" className="text-xs text-fg-muted font-medium hover:text-fg transition-colors">
            Admin
          </Link>
          <Link href="/dashboard" className="text-xs text-fg-muted font-medium hover:text-fg transition-colors">
            Dashboard
          </Link>
        </div>
      </div>
      {/* User avatar */}
      <div
        className="w-[26px] h-[26px] rounded-full bg-brand-muted text-brand font-bold text-[10px] flex items-center justify-center flex-shrink-0"
        aria-label={`User: ${name}`}
      >
        {initials}
      </div>
    </nav>
  )
}
```

**src/app/(admin)/layout.tsx** — Create new file (does not exist). Import TopNav and wrap children:

```tsx
import TopNav from '@/components/layout/TopNav'

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return (
    <>
      <TopNav />
      <main className="min-h-screen bg-bg">{children}</main>
    </>
  )
}
```

**src/app/(employee)/layout.tsx** — Create new file (does not exist). Same pattern as AdminLayout but named EmployeeLayout.

These layouts cover (admin) and (employee) route groups only. No TopNav is added to (auth), (leadership), (manager), survey/public/, or results/ routes — those are out of scope per the spec.
  </action>
  <verify>
    <automated>cd /Users/xavierpraveen/Documents/Projects/surveyer && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>src/components/layout/TopNav.tsx exists as an async RSC fetching the user and rendering a nav with logo + links + avatar using only design token classes. (admin)/layout.tsx and (employee)/layout.tsx both exist and wrap children with TopNav + main.bg-bg. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Run `npx tsc --noEmit` from project root — zero TypeScript errors
2. Run `npm run build` or `next build` — zero build errors
3. Inspect globals.css: count CSS custom properties — should be exactly 21 under :root
4. Inspect tailwind.config.ts: confirm all 21 tokens present, letterSpacing.snug/tight, boxShadow.sm/md, borderRadius.DEFAULT = '6px'
5. Confirm (admin)/layout.tsx and (employee)/layout.tsx exist and import TopNav
6. Confirm TopNav is NOT present in (auth)/layout.tsx, (leadership)/layout.tsx, (manager)/layout.tsx, survey/public/, or results/
</verification>

<success_criteria>
- globals.css: 21 CSS vars in :root + reduced-motion block
- tailwind.config.ts: full token map, Inter fontFamily, tracking-snug/tight, shadow-sm/md overrides
- layout.tsx: Inter loaded via next/font/google, inter.variable on html
- TopNav.tsx: RSC, logo gradient, "Surveyer" wordmark, nav links, user initials avatar — all token classes
- (admin)/layout.tsx and (employee)/layout.tsx both exist and use TopNav
- Zero TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-brand-redesign/05-01-SUMMARY.md`
</output>
